export default function SourceList({ sources }: { sources: Array<{ name: string; url: string }> }) {
  return <section className="mt-14 border-t border-white/10 pt-8" aria-labelledby="sources-heading"><h2 id="sources-heading" className="text-xl font-extrabold">Sources and verification</h2><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-400">Primary-source links were checked on 11 August 2026. External pages and platform terms can change. For viewing links, verify region, price, access, and replay windows at the destination.</p><ul className="mt-5 space-y-3">{sources.map(s=><li key={s.url}><a href={s.url} target="_blank" rel="noopener noreferrer" className="inline-flex min-h-8 items-center text-sm font-bold text-red-400 underline decoration-red-400/30 underline-offset-4 hover:text-red-300">{s.name}<span className="sr-only"> (opens in a new tab)</span></a></li>)}</ul></section>;
}
