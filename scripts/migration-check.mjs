const base=(process.argv[2]||'http://127.0.0.1:3000').replace(/\/$/,'');const tests=[['/world-cup-2026-iptv/',308,'/world-cup-2026'],['/world-cup-2026-iptv/matches/mexico-vs-south-africa/',308,'/world-cup-2026'],['/world-cup-2026-iptv/teams/mexico/',308,'/world-cup-2026/teams'],['/world-cup-2026-iptv/usa/',308,'/world-cup-2026/replays/usa'],['/uk/',308,'/world-cup-2026/replays'],['/index.html',308,'/'],['/channels',308,'/live-tv'],['/live-channels',308,'/live-tv'],['/blog/world-cup-final-2026',308,'/world-cup-2026/final'],['/checkout',410,''],['/blog/definitely-removed-iptv-offer',404,''],['/definitely-not-a-page',404,'']];let bad=0;for(const [path,status,location] of tests){const r=await fetch(base+path,{redirect:'manual'});const got=r.headers.get('location')||'';const ok=r.status===status&&(!location||got===location);console.log(`${ok?'✓':'✗'} ${path}: ${r.status}${got?` -> ${got}`:''}`);if(!ok)bad++}if(bad)process.exit(1);console.log('Migration status and redirect checks passed')
